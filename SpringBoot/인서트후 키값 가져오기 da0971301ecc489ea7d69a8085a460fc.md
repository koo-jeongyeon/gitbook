# 인서트후 키값 가져오기

생성일: 2021년 1월 14일 오후 3:02

```xml
<![CDATA[
			INSERT INTO hall
				( kind
				, qr
				  , rname)
			VALUES (#kind#
					 , #qr#
			 	  , #instruction#
				  , #btitle#
				  , #rname#)
		]]>
		
 	  <selectKey keyProperty="idx" resultClass="Integer">
        SELECT LAST_INSERT_ID()
      </selectKey>

//=====================================

int idx  = (int)sampleDAO.insert("insertHall",in);

마지막 인덱스 가져오기

<selectKey keyProperty="date2" resultClass="String">
			SELECT date FROM homebuild where idx = LAST_INSERT_ID()
		</selectKey>
```